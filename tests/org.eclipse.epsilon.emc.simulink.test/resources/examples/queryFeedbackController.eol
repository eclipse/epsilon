var a = SubSystem.all;
assert( a.size() > 0 );

var inportBlock = Inport.all.println("Inport:").first(); // Block kind Inport Type
inportBlock.Name.println("name: ");
var port = Port.all.println("Port:").first(); // Port Kind
port.Name.println("name: ");
var input = inport.all.println("Input:").first();  // Port Kind Input Type
input.Name.println("name: ");
var lines = Line.all.println("Line:").first();  // Line kind
lines.Name.println("name: ");

/*
for (subsystem in a) {
	
	("Exploring subsystem: " + subsystem.name).println();
	
	for (child in subsystem.getChildren()) {
		assert(child.name <> null);
		assert(child.name.println() <> "");
		if (child.isTypeOf(Inport)) {
			("Inport: " + child.name).println("\t");
			("To: " + child.outports[0].lines[0].destination).println("\t\t");
		}
		else if (child.isTypeOf(Outport)) {
			("Outport: " + child.name).println("\t");
			("From: " + child.inports[0].lines[0].destination).println("\t\t");
		}
		else {
			
		}
	}
}*/
